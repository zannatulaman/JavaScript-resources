const fruits = ["Apple", "Banana", "Orange"];
console.log(fruits.length);


fruits[1] = "Grape"
console.log(fruits[1]);

const numbers = [1, 2, 3, 4, 5,];

// numbers[6] = 20;
// numbers.push(30);
// console.log('After pushing;',numbers)
// numbers.pop();
// console.log(numbers)

numbers.shift(3);
numbers.unshift(0);     //to add the number in the beginning of the array
console.log(numbers);

