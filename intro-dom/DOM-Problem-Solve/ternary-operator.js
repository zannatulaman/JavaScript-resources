const age = 18;

const isAdult = age >= 18 ? "Yes" :"NO";
console.log(isAdult);

let number = 5
let result = (number > 0)? "positive" : (number< 0)? "negative" : "zero"
console.log(result);

const number2 = -3;
const isResult = (number2> 0)? "Positive" : (number2 < 0 )? "negative" : "zero"
console.log(isResult);

const score = 76;
const grade = (score >= 90) ? "A" : (score >= 80) ? "B" :  (score >= 70) ? "C" :  (score >= 60) ? "D" :
console.log(grade);

const a = 5;
const b = 10;

const largerNumber = (a + b >= 15)? "Larger" : (a + b < 10)? "Fewer" : "zero"
console.log(largerNumber);


let isMember = true;
let originalPrice = 100;

let finalPrice = isMember ?  "Discount 20%" : "Discount 10%"
console.log(finalPrice);

               
              
              
              
